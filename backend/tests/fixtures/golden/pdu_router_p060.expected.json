{
  "fixture": "pdu_router_p060",
  "document_key": "pdu_router",
  "source_document": "AUTOSAR_CP_SWS_PDURouter.pdf",
  "page": 60,
  "labeled": "hand-verified against the rendered PDF page — never regenerate from code",
  "notes": "The corpus's newest casing quirk, pinned: [SWS_PDUR_00816] is genuinely spelled UPPERCASE in the source (the §7.8.2 runtime-errors table — the document's only such id), directly next to the normally-cased [SWS_PduR_00100] (§7.8.1 development errors). A req_id_pattern without the PDUR alternative, or a lookup that folds case, silently drops 00816 while every PduR-cased fixture stays green. Both are the titled variant with table bodies, and both titles carry the spec's own 'Definiton' typo, transcribed verbatim. 00100 closes with three SRS_BSW upstream refs; 00816 closes with an empty ⌋(). The stray closer at the top of the page belongs to SWS_PduR_00504, whose id and opener sit on page 59 — it is not labeled here.",
  "requirements": [
    {
      "id": "SWS_PduR_00100",
      "title": "Definiton of development errors in module PduR",
      "text": "Type of error Related error code Error value Invalid configuration pointer PDUR_E_INIT_FAILED 0x00 API service (except PduR_GetVersionInfo) used without module initialization or PduR_Init called in any state other than PDUR_UNINIT PDUR_E_UNINIT 0x01 Invalid PDU identifier PDUR_E_PDU_ID_INVALID 0x02 If the routing table is invalid that is given to the PduR_EnableRouting or PduR_DisableRouting functions PDUR_E_ROUTING_PATH_GROUP_ID_INVALID 0x08 Null pointer has been passed as an argument PDUR_E_PARAM_POINTER 0x09",
      "upstream_ids": ["SRS_BSW_00337", "SRS_BSW_00323", "SRS_BSW_00452"],
      "named_symbols": ["PduR_GetVersionInfo", "PduR_Init", "PduR_EnableRouting", "PduR_DisableRouting"],
      "page": 60,
      "spans_page_break": false
    },
    {
      "id": "SWS_PDUR_00816",
      "title": "Definiton of runtime errors in module PduR",
      "text": "Type of error Related error code Error value TP module rejected a transmit request for a valid PDU identifier in case of gateway operation PDUR_E_TP_GW_TX_REQ_REJECTED 0x03 Loss of a PDU instance (buffer overrun in gateway operation) PDUR_E_PDU_INSTANCES_LOST 0x0a",
      "upstream_ids": [],
      "named_symbols": [],
      "page": 60,
      "spans_page_break": false
    }
  ]
}
